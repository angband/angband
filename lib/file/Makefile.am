## Makefile.am -- Process this file with automake to process Makefile.in

angband_files = \
  dead.txt news.txt

EXTRA_DIST = \
  $(angband_files)

if SET_GID
angbanddir = @DEFAULT_PATH@file

angband_DATA = \
  $(angband_files)
endif

